var function AbBack (...abStack) {
    this.next;
    this.lastNext;
    this.config = abStack.shift();
    this.stack = ab.Stack.reverse();

    return function (req, res, lastNext) {

    }
};

module.exports = AbBack;
